# RootMe

**Platform:** TryHackMe  
**Difficulty:** Medium
**OS:** Linux
**Category:** Web / PrivEsc / Enumeration  
**Date Completed:** 09-Feb-2026  

## 🔍Recon
### Nmap Scan
PORT    STATE  SERVICE

22/tcp  closed ssh

80/tcp  closed http

443/tcp closed https



### Gobuster
![](attachments/Pasted%20image%2020260210193351.png)

#### Observations
`robots.txt` might contain some information.
`/admin` brings me to `admin/index.html` which appears buggy and keeps refreshing
`/atom` downloads a file with a `.atom` extension
`/login.php` might be a place for brute forcing
most of the other directories are forbidden as I do not have permission

## 💥Exploitation 
I start by examining page source and also the `robots.txt` file mentioned earlier.

In `robots.txt` the following is found
```
User-agent: *
fsocity.dic
key-1-of-3.txt
```

`fsocity.dic` seems to be a dictionary of random words, however `key-1-of-3.txt` contains the first flag.

`fsocity.dic` might contain a password that can be used to login through SSH or the wordpress login. 



On the `wp-login.php` page, the following is shown, I tried `test` as both username and password and the error popped up. I may be able to test for a correct Username and Password using this error and the previous `fsocity.dic` file.

![](attachments/Pasted%20image%2020260210221515.png)


I start by capturing a login request with burp suite.
![](attachments/Pasted%20image%2020260210221525.png)

Then I send it to intruder and add the `log` position only as I want to find a valid username. I then set the payload using the fsocity.dic file from earlier.
![](attachments/Pasted%20image%2020260210222003.png)

Filtering by both Response received and length, I found some possible candidates:

mrrobot - high response received
Elliot - High Length

I tried Elliot on the login page with a test password and seems that Elliot is a valid username.
![](attachments/Pasted%20image%2020260210223033.png)

However it does not seem that mrrobot is valid, I will keep this in mind as this may be a SSH username instead.

Now that I have a username, `Elliot`. I used the same steps but for the password field.


I added the position, remembering to change `log` as Elliot and put the previous fsocity.dic as the payload.

![](attachments/Pasted%20image%2020260210223404.png)

# Conclusion

# References
