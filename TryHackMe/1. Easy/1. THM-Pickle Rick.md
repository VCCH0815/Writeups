*02/02/2026*

# Enumeration

### Wappalyzer
![](attachments/Pasted%20image%2020260202181150.png)

### Page Source
From the page source of the index page, I found a comment.
```
<!--
    Note to self, remember username!

    Username: R1ckRul3s
  -->
```

### NMap 
PORT   STATE SERVICE
22/tcp open  ssh
80/tcp open  http

### Gobuster
`gobuster dir -u "10.48.191.166" -w /usr/share/wordlists/SecLists/Discovery/Web-Content/common.txt`

/.hta                 (Status: 403) [Size: 278]
/.htpasswd            (Status: 403) [Size: 278]
/.htaccess            (Status: 403) [Size: 278]
/assets               (Status: 301) [Size: 315] [--> http://10.48.191.166/assets/]
/index.html           (Status: 200) [Size: 1062]
/robots.txt           (Status: 200) [Size: 17]
/server-status        (Status: 403) [Size: 278]

### Dirsearch
/assets 
/assets/
/login.php
/robots.txt

# Exploitation 
From Gobuster, I found a robots.txt and upon accessing it I got the following.
`Wubbalubbadubdub`

This may be a password.

I used both the Username and Password on the login.php page and successfully accessed the web portal.
![](attachments/Pasted%20image%2020260202200532.png)

Entering `ls` revealed a list of files and directories.
```
Sup3rS3cretPickl3Ingred.txt
assets
clue.txt
denied.php
index.html
login.php
portal.php
robots.txt
```

I tried to use `cat` to list out the first flag but an error came out.
`cat Sup3rS3cretPickl3Ingred.txt`
![](attachments/Pasted%20image%2020260202200740.png)
I googled some alternatives to cat and found a few commands that could work, For example, `less, more, tac, head, grep`. I used less and successfully got the first flag. 

There is also a `clue.txt` which had the following.
`Look around the file system for the other ingredient.

Seems like the second ingredient is somewhere in the filesystem.

I tried accessing the other tabs but the following showed up.
![](attachments/Pasted%20image%2020260202201200.png)

but since I have a command line, i can try to get both /etc/shadow and /etc/passwd.

# Getting a reverse shell
I started by doing `python3 --version` to check if the machine has python installed. Then, I used the python reverse shell cheatsheet at https://pentestmonkey.net/cheat-sheet/shells/reverse-shell-cheat-sheet. 

I set up a netcat listener using `nc -vlnp 9999`.

and executed the code on the web portal.

It was unsuccessful at first but I remembered that the machine is running python3, not python, so I had to modify the code.

Sure enough, I got a reverse shell on the machine.


# Privilege Escalation
Seems like I do not have enough privilege so I start to escalate. I started to try and get more usernames and passwords.

To get more usernames, I first started getting the /etc/shadow and the /etc/passwd file. 

Using `less /etc/passwd`, I got the list of users, to filter for real users, I used the command `less /etc/passwd | grep home` and got the following.
```
syslog:x:104:108::/home/syslog:/bin/false
ubuntu:x:1000:1000:Ubuntu:/home/ubuntu:/bin/bash
```

# Stuck
Got Stuck here so I looked for help. Previously I used `sudo -l` to list out what my current user can use with sudo and got the following.

`(ALL) NOPASSWD: ALL` 

This means that my current user can run all commands with sudo, which I didn't know.

# Getting flags
I immediately tried sudo su and successfully switched to the root user. 

under `/root` was the 3rd flag. 

And under /home/rick was to folder for the second flag. The second flag was in a file named `Second Ingredient` which cannot be accessed with normal ways. To access a file with a space in the name, I used `cat ./"Second Ingredients"`.

# End
I should always enumerate. A problem I encountered was I didn't fully enumerate the directory of the web server, I used an incorrect wordlist that did not include files such as `robots.txt` and `login.php`.

I should use `sudo -l` more to list what my current user can do.
